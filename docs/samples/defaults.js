import {defaults} from 'chart.js-v3';

defaults.set({
  plugins: {
    legend: {
      display: false
    },
    title: {
      display: false
    },
    tooltip: {
      enabled: false
    }
  }
});
